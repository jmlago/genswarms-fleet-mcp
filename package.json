{
  "name": "genswarms-fleet-mcp",
  "version": "0.1.0",
  "description": "MCP server for a fleet of GenSwarms swarms: observe (dashboard), configure (schema-gated overlays), operate (opt-in) — from any MCP harness",
  "license": "MIT",
  "type": "module",
  "bin": {
    "genswarms-fleet-mcp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jmlago/genswarms-fleet-mcp"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^5.6.0"
  }
}
